#include <stdio.h>
#include <iostream>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>
#include <string>
using std::string;

using namespace std;

void syntatic(string tokens)
{
    int estado = 0;

    std::cout << tokens << '\n';
    // switch (estado)
    // {
    // case /* constant-expression */:
    //     /* code */
    //     break;

    // default:
    //     break;
    // }
}
